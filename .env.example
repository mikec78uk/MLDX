# Which brand skin to build/serve: "range-rover" | "defender"
NEXT_PUBLIC_BRAND=range-rover

# Optional Basic Auth gate for shared/Vercel deployments.
# Leave both unset locally — middleware is a no-op without SITE_PASSWORD.
SITE_USERNAME=preview
SITE_PASSWORD=
